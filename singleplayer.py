import calc
import card
import player
import move
import random

def start_game(player):
    cards = []
    cards = calc.initialize_cards(cards)
    moves = []
    moves = calc.initialize_moves(moves)
    #got cards
